# 개발·검증 교훈

- 시계 주입은 최상위 판단 함수뿐 아니라 TTL·감쇠·근거 만료까지 연결한다. 과거 스냅샷 재생에 실행 시각을 사용하지 않는다. naive KST와 aware UTC를 비교할 때 단순 tzinfo 교체로 같은 시각인 척하지 않는다.
- API 호출이 예외 없이 끝났다는 사실과 자료 획득 성공은 다르다. 정상 0/중립은 보존하고 실패 기본값은 성공 캐시에 넣지 않는다.
- 의존성 부재 경고를 고칠 때 패키지만 설치하지 않는다. 죽은 소비 경로·서버 실행 조건·응답 단위·신호 가산 활성화 여부부터 확인하고, 폐기 시 미획득을 정상 검증으로 바꾸지 않는다.
- 집계의 0이 무보정·기권을 뜻하면 예측 평가에서 보합 주장으로 세지 않는다. 커버리지와 제외 사유를 보존한다.
- 최신 종목별 표시 캐시는 과거 판단 평가의 정본이 아니다. append-only 심의 원장과 ID를 사용하고, 같은 가격경로가 분모에 중복되지 않게 한다.
- 체결 여부를 잠시 확인할 수 없으면 기존 측정값을 삭제하지 않고 평가 참여 상태만 분리한다. 관측 복구가 과거 수익률 재가격화를 일으키지 않는지 저장·재로드까지 검증한다.
- 조회 창 밖의 미완성 자료를 계속 보존할 때는 호출 상한을 독점하지 않는지도 검증한다. 실패 시도 이력을 영속화해 재시작 뒤에도 신규·기존 후보가 조회 기회를 얻도록 한다.
- 로컬 검증은 격리 워크트리에서 외부 API·운영 자격증명 없이 수행한다. 전체 테스트의 배포 스크립트 fixture는 가짜 SSH를 사용하며 `env -i` 환경에 비운영 `QWQ_DEPLOY_SSH_KEY`를 명시할 수 있다. 운영 재시작·배포는 별도 명시적 승인 사항이다.
- 입력 값/digest가 같아도 중간에 A→B→A가 없었다는 증거는 아니다. 관련 정책의 실제 commit 변경 이력을 함께 고정하고, 무관한 체결/ACK의 전역 version 변화로 판단을 취소하지 않는다. restore 검증에는 현재 counter뿐 아니라 당시 seal/완료 직전 이력이 필요하다.
- 완료 전 입력 동일성 검사와 완료 후 계속 유효한 소비 권한은 다르다. 최신 source의 새 시작·실패가 실제 snapshot/최종 소비자까지 전파되는지 별도로 검증한다. 계산 parity·기반 모듈 승인만으로 실제 writer 이행이나 거래 허가를 선언하지 않는다.
- 복구 래치는 아무 작업의 성공으로 해제하지 않는다. 실패한 명령·세대와 실제 새 commit/게시 성공을 결합하고, 중복 결과 조회·다른 종목 성공·결과 불명을 구분한다. 저장소 write가 성공한 뒤 게시만 실패할 수 있으므로 예외를 미저장 증거로 사용하지 않는다.
- 보호 결정의 pending은 정상 quote→prepare 창과 재시작 고아가 같은 저장 모양일 수 있다. 해제 reducer만으로 안전을 선언하지 말고, 단일 소비자의 직렬화·취소·종료 배수까지 인수한다.
- 미적용 체결은 미체결이 아니다. terminal과 applied=0만으로 보호 pending을 지우지 않고, 관측 누적도 확인한다. reconcile→경제 적용 사이를 실제 큐로 재현해 수량뿐 아니라 보호 단계·원 intent가 유지되는지 검사한다.
