# 09-23 제한 운영 릴리스 — Plan → Do → See

## 의도와 범위

사용자는 main 병합·운영 배포·재시작 후 health/경보 연결·누적 상태 성능 검증을 요청했다.
사용자는 후속 답변으로 주문·전략·위험 설정 유지, 검증된 수정본만 배포를 확정했다.
거래/잔고는 KIS, Toss는 조회
전용이며 만료된 관측 권한을 연장하지 않는다.

기준 main `afa6e1e`, 운영 후보 PR #90 `202d52b`, 미승격 개발선 `5a2fab8`.
전체 개발선은 265파일 +79,070/-592줄이며 WS parser·공용 sizing/레짐·DB DDL 등은
owner 미설치에서도 실행된다. N3의 5778 통과와 좁은 리뷰를 whole-engine 운영 승인으로
확대하지 않는다. 전체 C/F/G/R·설치 차단을 유지하고 PR #90만 좁게 반영한다.

기존 배포 스크립트의 평문 인증 폴백을 조회 중 노출했다. 값은 이 문서·시험·외부 검토에
옮기지 않으며 사용하지 않는다. 실행 경로 제거와 계정 인증정보 교체는 다른 일이다.
기존 Git 이력/문서의 값 폐기·교체는 별도 보안 후속이며 계정 권한을 임의 변경하지 않는다.

## Global Constraints

- 전체 엔진 브랜치를 main에 통째 병합하거나 runtime/readiness를 활성화하지 않는다.
- 설정·위험·면제·주문·Toss grant는 명시적 대상/목표값 없이는 변경하지 않는다.
- 작업자는 실API/SSH/systemctl/자격증명·운영 상태에 접근하지 않는다. 운영 실행은 coordinator만 한다.
- 비밀을 출력·새 파일·리뷰 diff에 복사하지 않는다. 기존 민감행을 포함한 diff도 마스킹한다.
- 전체 pytest는 모든 native/외부 작업자 종료 뒤 coordinator가 UTC→KST 직렬 실행한다.
- native 요청 모델/effort와 실제 확인 metadata를 구분한다. 작성자는 최종 승인하지 않는다.
- 이번 배포 인증 수정의 외부 검토 예산은 최대2회, 각600초/USD5, Opus5/xhigh/tools0다.

## Task 1: local deploy의 비대화형 인증 경계

구현자 Astra/high. WT `release-auth-impl-20260923`, base `202d52b`.
허용 파일: `scripts/deploy/local_deploy.sh`, 신규 `tests/deploy/test_local_deploy_auth.py`만.
문서는 coordinator 소유다. 시간상한20분·fanout0·전체 시험 금지, focused만 실행한다.

- [x] 기존 deploy focused 기준선을 확인하고 새로운 실패 경로를 RED로 고정한다.
- [x] 평문/표준입력/대화형 인증 폴백을 완전히 제거한다. sudo는 항상 `-n`만 사용한다.
- [x] 서비스 재시작 권한을 비파괴 목록 검사로 확인한다. 실패하면 fetch/checkout/verify/
  restart/rollback 이전에 nonzero로 종료해 운영 checkout/서비스를 건드리지 않는다.
- [x] 권한 확인 성공 뒤 실제 restart 실패도 정상으로 보고하지 않는다. 기존 실패 시
  이전 SHA 복원·재시작·health 및 복구실패 exit2 의미를 유지한다.
- [x] 정상 배포·권한 거부·재시작 시점 권한 소실·롤백 성공/실패를 실제 script로 인수한다.
  temporary Git repo와 외부 경계 command double만 사용한다. 실제 sudo/네트워크0.
- [x] test가 운영 lock을 열지 않도록 기존 remote 스크립트와 같은
  `QWQ_DEPLOY_LOCK_FILE` 재정의를 지원하되 기본 lock 경로는 유지한다.
- [x] 인증값을 patch/출력에 넣지 않으려면 apply_patch로 해당25행 스크립트를 삭제 후
  비밀 없는 전체 내용으로 재생성한다. diff는 민감행을 마스킹해서만 확인한다.
- [x] focused GREEN·bash syntax·자체 diff 검토 뒤 허용 파일만 커밋한다.

승인된 interpreter는 `/home/ubuntu/projects/qwq-ai-trader/venv/bin/python`이다.
명령 환경은 `env -i PATH=/usr/bin:/bin LANG=C.UTF-8 TZ=UTC PYTHONDONTWRITEBYTECODE=1
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 QWQ_DEPLOY_SSH_KEY=/tmp/qwq-offline-no-ssh-key`를 사용하고
`-m pytest tests/deploy -q -p no:cacheprovider --tb=short --show-capture=no`로 제한한다.

## Task 2: 검토·통합·main 릴리스

- [x] Task1 독립 Astra/xhigh 및 tools-off Opus/xhigh 검토, 지적별 재현/수정/재검토.
- [x] PR90의 최신 head/CI·범위 검토, 인증 수정 결합 후보 전체 UTC/KST·패턴 검사.
- [ ] PR 경유 필수 verify 통과 후 main 병합. 보호 브랜치 직접 push/검증 우회 금지.
- [ ] 장외·pending0·clean·서비스/health·설정/킬스위치 지문을 확인한 뒤 고정 SHA 배포.
- [ ] 롤백 가능성을 보존하고 재시작 후 새 PID·동기화·health·추가 KIS 호출 여부를 점검.
- [ ] 운영 동작과 장중 미관측 항목을 구분해 문서/커밋/푸시한다.

## Task 3: 다음 개발 경계

운영 배포가 정상일 때 개발 정본에서 N3→health/경보 소비 계약과 합성 누적 부하를
설계·검증한다. legacy에는 owner 없음으로 인한 유령 경보를 만들지 않고 unknown/
volatile/in-flight/건수 미완전을0건으로 표현하지 않는다. 운영 owner 설치와는 별개다.
전체 엔진 main 승격과 자동 복구가 완료됐다고 보고하지 않는다.

## See

### 구현·기준선

구현자 Astra/high `5b5bab0`를 coordinator가 `d3c809f`로 가져왔다. 두 파일만 변경했다.
기존 deploy4 passed, 합성 RED7 failed/4 passed, 최종 GREEN11 passed(1.54초/rc0/격리0).
RED에서 인증값과 기본 lock을 메모리에서 합성값/tmp로 바꾼 뒤 복사했고 원값은 쓰지 않았다.
최종 GREEN은 제품 원본 script+환경 lock 재정의를 실행했다.
coordinator도 main 병합 후11 passed(1.52초/rc0/격리0), bash-n/diff-check rc0을 확인했다.

### 독립 검토와 main

- 별도 Astra/xhigh 정적 리뷰: `d3c809f` 허용4파일, P0/P1/P2 차단0. actual/effective
  metadata 미노출이며 테스트 실행으로 세지 않는다. lock 실패/경합 시험은 비차단 보완 제안.
- 외부1/2는 실제 `claude-opus-5`·요청xhigh·tools0·terminal success·child/runner rc0,
  **274.819초/USD0.709745/APPROVE**다. 입력24,449bytes·SHA256
  `d2869c0b493884716459ebba7ecea28bfb7a2cafb72ad31891acd7d0ed997451`.
  신규 차단0, 기존 한계/비차단 제안8건. 실행 시험이 아닌 첨부 정적 검토다.
  effective effort는 미확인. 두 번째 호출은 사용하지 않았다.
- PR #90 head `202d52b`, 필수 verify SUCCESS(run35851346885), 제품9파일을
  coordinator가 재검토하고 09-23 23:01:41 KST main `4db5141`에 merge commit 병합했다.
  인증 수정 branch는 이 main을 병합(`f2bcd05`)했다. 기존 서비스는 아직 변경하지 않았다.
- 현행 local deploy의 인증 폴백은 세 트리에 동일했다. 최초 inventory의
  password/sshpass 검색만으로 '폴백 없음'이라는 결론은 폐기했다. sudo -S 불리언 검증으로
  정정했으며 원문 값은 출력하지 않았다. remote deploy와 local deploy를 혼동하지 않는다.

### 남은 검증/운영

후보 `f2bcd05`(문서 외 제품/시험 불변)의 coordinator 단독 전체 UTC는
**2076 passed/기존2 xfailed/기존pykrx warning1/76.75초/rc0/격리0**다.
제품 tree `8172c88d86bd6192de870826b1cc5949a3141716`, tests
`8b0ef5025ef708247ec3ca8409c81986256fd4dc`, scripts
`3867815fc72724fa24193b81e81c65563a486c3d`. 모든 native/외부 작업자 종료 후 직렬 실행했다.
정적 verify는 tests-skip으로 별도 실행해 문법·제한된 비밀 패턴 검사 rc0을 확인했다.
저장소 전체에 비밀이 없다는 보장은 아니며 이력/문서의 기존 민감값 후속은 유지한다.
KST도 **2076 passed/기존2 xfailed/기존warning1/79.82초/rc0/격리0**다.
서로 다른4152개 시험으로 합산하지 않는다.

새 PR CI·고정 main SHA 배포/재시작·사후 점검은 진행 중이다.
현재 read-only 권한 목록 검사는 rc0이며 운영 PID2259506/Toss inactive를 확인했다.
이는 실제 restart 성공이나 장중 관측 완료의 근거는 아니다. 장중 매도 이유/지수 freshness/
호출 주체별 거절률은 자연 발생 표본으로 후속 검증하며 시험 주문을 내지 않는다.

### 비차단 의견 처분

- `PREV` 조회 실패/checkout 실패 때 복구 재시작/광범위 target/기본 tmp lock/더티 등
  기존 경로의 추가 인수는 별도 배포 hardening 후속이다. 이번 승인 범위를 넓히지 않는다.
- exit1은 사전검사 실패도 포함하며 복구 완료의 증거가 아니다. 호출 결과의 `[복구]` 및
  실제 HEAD/PID/health를 함께 확인한다. scripts/.github 검색에서 local deploy 자동 호출자0을 확인했다.
- 현 호스트의 목록 조회 권한을 확인했고 runbook에 필요 권한을 명시한다.
- lock override는 이번 실배포 환경에 넣지 않는다. 새 PID와 기동 확인을 별도 수행한다.
- 테스트 git setup의 전역 설정 배제·lock/인자/더티 실패 회귀는 비차단 추가 시험으로 남긴다.
  현재 승인된 env-i 명령·실격리 검사를 따르며 GREEN을 sudoers/systemd 인수로 해석하지 않는다.
