# 토스 도입 설계 Codex 교차 리뷰 (2026-09-15)

## 결론과 범위

Claude 검토본 `85a4266`은 주요 제약을 잡았지만 **그대로 구현하면 안 되는 잔존 모순**이 있었다. 본 변경은 아래 14개 보완 항목(P1 4개·P2 10개)을 설계/인수 명세에 반영한다. **설계 결함의 정정이며 아직 없는 Toss 구현의 버그를 재현·수정했다는 뜻은 아니다.** 토스 구현·토큰 발급·인증 조회·운영 배포/재시작·주문·설정 변경은 하지 않는다.

검토 근거:

- 최신 main `8849d92`(소스 변경 최종 PR #63 `c9923bf`)와 `85a4266`의 전체 설계/문서 diff. Claude 원 브랜치 `fix/toss-design-review-followup`은 원격과 동일하고 clean이었다. main과는 main 쪽 7개/설계 쪽 1개 커밋 차이였다.
- 초안 `327d866`은 PR #60 `4b70a34` 이력에 포함되어 있다. 문서 혼입은 Git으로 확인되지만, 혼입 당시 명령/의도는 사용자 제공 Claude 보고에 의존한다.
- 기존 Claude 커밋을 최신 main 기반 feature 브랜치에 cherry-pick하여 보존했다. CHANGELOG 충돌은 **MCP 후속 기록과 Toss 기존 검토 기록을 모두 유지**해 해결했다. 원 브랜치·운영 checkout은 변경하지 않는다.
- 인증 없는 [공식 OpenAPI JSON](https://openapi.tossinvest.com/openapi-docs/latest/openapi.json), `info.version=1.2.17`을 확인했다. 기존 실측 TPS·IP·시세 차이·30일 로그 수치는 독립 실측하지 않고 이전 보고로 구분했다. AsyncAPI/약관/공급자 응답은 이번에 검증 완료한 것으로 주장하지 않는다.

## 발견 사항 → 설계 보완

아래 원문 위치는 **`85a4266` 기준**이다. 경로는 모두 해당 커밋 또는 main `8849d92`로 확인하며, 후속 편집본의 줄번호와 혼동하지 않는다.

| ID | 수준 | 근거 / 실패 시나리오 | 반영 위치 |
|---|---|---|---|
| R01 | P1 | 원문 §4.2 156~158행: revoked 재발급 금지인데 캐시가 같으면 발급. 외부 발급/저장 실패 시 상대 최신 토큰을 다시 폐기 | §4.2 단일 issuer/reader·동일/미획득 캐시 mint0·재시작 지속 auth 회로·불명확 발급 결과 중단; A01/02 |
| R02 | P1 | `src/utils/atomic_io.py:23`의 고정 `.tmp`/기본 권한은 umask022에서 0644가 될 수 있어 토큰0600 계약 불충족 | §4.2 전용 보안 writer·소유권/symlink/초기0600·안전 게시; A03 |
| R03 | P1 | 원문 §6.5/§8의 read-only는 의도뿐. 동일 bearer와 범용 HTTP를 쓰면 금지 endpoint 오호출 가능 | §6.5 메서드+경로 allowlist·고정 origin/TLS·redirect 금지·issuer 전용 POST; A04 |
| R04 | P1 | 원문 142~145행 opt-in/청산 금지 ↔ 233~236행 전역 브로커 훅/정규장 허용 충돌 | §0/4.1/4.6/Phase3/6.1 전 세션 돈 경로 제외·wrapper만 허용; A12 |
| R05 | P2 | 원문 §3.3/4.4: None 대신 숫자만 넣어도 충분하지 않음. 오늘 봉 없이 최근2봉을 오늘/전일로 조립하면 등락률·OHLCV 오염 | 내부 nullable 결과와 legacy 계약 분리, 필드별 충분성·거래일·관측/수신/부분봉 계약; A08/09 |
| R06 | P2 | 원문 Phase2 223행: 200봉으로52주 계산 ↔ §7 추가 페이지 필요 모순. 201~250번째 고점 누락 | 200=페이지 크기, 소비자별 전체 구간·nextBefore·중복/실패/짧은 이력 처리; A10 |
| R07 | P2 | 원문 §4.3 retry2 ↔ §6.6 retry1. HTTP timeout 밖 락/429 대기·중첩 retry·취소가 전체 지연을 제한하지 못함 | 공유 monotonic deadline·총 조회 retry1·발급 retry0·취소/회로/rollback; A07 |
| R08 | P2 | `src/utils/kis_rate_limit.py:39`는 프로세스 내부 공유. 복제만으로 토스 전체 자격 예산을 보장 못함 | 단일 송신자·동시 reader 조회 금지, 향후 공유 게이트 필요, Reset의 '1토큰 보충까지 초' 해석; A06 |
| R09 | P2 | `supply_score.py:65/111`, `sector_momentum.py:385`는 출처 없는 날짜/TTL 캐시 소비. 파일 재사용이면 shadow가 live로 샐 수 있음 | §4.5 소스/모드/시장/조정/기간 namespace·off 읽기 차단·부분봉 확정 분리; A11 |
| R10 | P2 | `src/agents/team_ledger.py:32/44` 마스킹은 Authorization/Bearer/JWT 일반 문자열까지 가리지 않음 | §6.5 허용 필드 로그·raw 인증/body/예외 금지·합성 비밀 검사; A05 |
| R11 | P2 | 원문 §2는 vol_inrt만 원인으로 쓰나 PR60 실제 원인은 화면코드. Phase0 3영업일 인수 미확인인데 완료 체크 | 이전 조사와 현재 코드 구분, 코드3건 완료/운영 기준선 미완 분리 |
| R12 | P2 | 공식 스펙: /stocks/all은 토스 거래 가능 목록; 종목 수급은 주·통합시장·등록외국인·잠정/후행 수정; 8개 시장 지표는 주가지수2+채권수익률6 | §3 커버리지/단위/point-in-time 계약; 마스터 삭제/무가산/미래 수정치 유입 방지; A13 |
| R13 | P2 | 가격 p95만으로 장애시 안전·거래소 동등성 증명 불가. 표본·분모·시간 정합·전체 페이지 인수 부족, '죽은 채널이라 저위험' 과소평가 | Phase1 manifest 사전 등록·실패/제외 분모·소비자별 승격, Phase2 shadow/중복 가산/기준선; A14 |
| R14 | P2 | Phase1 기본on·약관은Phase2부터 확인 ↔ 전체 승인 원칙 충돌. off 토큰 초기화/캐시 소비/간접 정책 변경 경계도 누락 | 모든 기본off·실자료 사전 조건·단계/consumer 승인 분리·기동/캐시/잡0; A12/14 |

### 실제 돈 경로 대조

- `src/schedulers/kr_scheduler.py:4686`의 REST quote는 `source="rest_polling"`으로 다시 표시되어 엔진 이벤트/청산으로 흐른다. 원 `source=toss` 문자열이 사라지므로 source 통계만으로 격리를 증명할 수 없다.
- `src/core/batch_analyzer.py:1756`은 quote로 portfolio current/highest를 갱신하고 `:1812`에서 ExitManager를 호출한다. 브로커 import 분리만으로 이를 차단하지 못한다.
- 스케줄러 `:4814`의 **'전광판'도 시장 이벤트를 발행**한다. 이름만 보고 표시용 allowlist에 넣지 않는다.
- 반대로 현재 dashboard SSE/HTTP가 직접 portfolio를 갱신한다는 주장은 확인되지 않았다. 해당 주장은 제외하고, 구현 PR에서 정확한 함수/하류 쓰기 계약을 요구한다.

### 결측/구간의 구체적 반례

- 화요일09:05 가격105, 확보 봉은 월요일100/금요일90: 직전 종가100이면 +5%다. 두 봉을 오늘/전일로 순서만 배정하면 +16.67%와 가짜 오늘 OHLCV가 만들어진다. 실제 `kr_scheduler.py:3715` 필터는 메타 없이 숫자를 소비하므로 wrapper 앞단에서 충분성을 확인해야 한다.
- 현재110, 최근200봉 고점120, 201~250번째 고점200: 고점 대비 −45%가 −8.33%로 잘못 계산될 수 있다. `core_screener.py:284/306/505`의 요청/최대값/필터와 대조했다. 이 리뷰는 기존 코어 전략을 수정하지 않으며, Toss가 불완전 구간을 새 52주 정본으로 확대하지 않도록 설계한다.

## 검토/검증 상태

- 병렬 읽기 전용 감사: Astra/high 토큰·보안·동시성, Astra/xhigh 소비자·돈 경로·시점, Terra/high Git/Phase0·필수 CLI. 부모는 공개 스펙·통합 문서 보완 담당이다.
- AGENTS가 지정한 `bash scripts/dev/codex_review.sh --branch`를 Claude 원 워크트리에서 read-only sandbox로 실행했다. **`bwrap … Operation not permitted`로 저장소 읽기에 실패**했다. CLI의 '병합 판단 보류'를 승인으로 세지 않으며 sandbox 우회는 하지 않았다. 위 직접 소스 감사는 이 CLI 실패와 별개의 Codex 에이전트 검토다.
- 문서 수정 전 격리 baseline: **1019 passed / 2 xfailed**, 24.47초, 운영 상태/외부 네트워크 접근 시도0, 문법/비밀패턴 검사 통과. 기존 pykrx DeprecationWarning 1건.
- 편집 후 전체 offline verify: **1019 passed / 2 xfailed**, 24.55초, 운영 상태/외부 네트워크 접근 시도0·문법/비밀패턴 검사 통과. xfail은 기존 손절 기준·익절 접촉 parity 차이이며 이번 문서 변경으로 해소하지 않았다. `git diff --check` 및 계획서/새 보고서를 포함한 변경 문서 별도 비밀패턴 검사도 통과했다. 넓은 `sk-` 패턴이 기존 `risk-sizing-…` 문서명에 일치해 단어 경계로 정정했으며, 실제 비밀 발견을 무시한 것이 아니다.
- 독립 재리뷰: Astra/high 보안7항 해소·신규 P1/P2 없음, Terra/high Git/상태/상대링크 오류 없음. Astra/xhigh 통합 리뷰에서 **A14가 표본0인 최초 관측까지 막는 순환 조건**을 찾아 관측 시작 승인과 승격 조건을 분리했고, REST 실제 함수명도 정정했다. 두 곳 재확인 후 **미해결 P0/P1/P2 없음, 문서 PR 승인**. 구현/운영 승인이 아니다.
- §10 A01~A14는 **향후 구현 인수 명세**이며 이번 통과 건수에 포함하지 않는다. 소스·테스트·설정·패키지 파일은 변경하지 않았다.

## 다음 작업과 승격 경계

1. 설계 보완본의 독립 재리뷰 후 **오프라인 Phase1 구현 승인**을 받는다. 토큰/HTTP 경계 → 데이터 계약/페이지/캐시 → shadow 배선 순으로, 파일 소유권이 겹치지 않는 작업만 병렬화한다.
2. Phase0 3영업일 기준선·계정 약관·단일 issuer 소유권·공급자 시장/조정 기준·사전 등록 manifest를 준비한다. 공개 스펙/합성 검증만으로 이 항목을 완료 체크하지 않는다.
3. 별도 실자료 관측 승인 후 shadow를 시작한다. Phase2 후보/정책 변경과 Phase3 표시 전용 폴백은 각자 근거/승인을 요구한다. 청산/사이징/호가/주문 전환·WS 실행 피드 이관은 이번 범위 밖이다.

이 문서는 운영 활성화 승인이 아니다. 구현이 없으므로 Toss의 실제 지연/장애 개선·투자 성능도 검증하지 않았다.
